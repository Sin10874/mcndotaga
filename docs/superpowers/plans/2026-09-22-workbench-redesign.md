# 画像工作台前端重做

用户要求：前端不够出色，需要重做，同时明确完整分析是否可用。基线为 63ca100，继续当前 GUI 分支与 PR #2。旧版本保留在该提交。

## 实施范围

- 改为深色战术分析界面，压缩查询区，突出战队、真实 BP 重点和选手图形。
- 总览增加英雄视觉、选手卡、五维雷达、对应位置百分位矩阵和真实覆盖摘要。
- 双人比较使用同轴双图，并继续说明跨位置不可解释为绝对强弱。
- BP 保留真实频率、手数筛选、机会分母；来源页列出当前已接入和未接入能力。
- 保留真实目录与 Profile 接口、筛选恢复、查询竞态控制、错误与空状态、JSON 预览复制。

## 实施边界

主控拥有 web/workbench.js、analysis/server.py 的图片 CSP、相关测试、文档、运行验收与 GitHub。独立前端实施者仅写 web/index.html 与 web/workbench.css。其他审查者只读，不连接数据库，不运行测试或进程管理。

只展示已有 Profile 支持的统计。禁止将跨位置百分位平均成综合强弱，禁止把历史 BP 频率称为预测或建议。缺维度时雷达不画闭合面积，零值正常显示。BP 并列最高保留并列，不制造唯一偏好。

英雄图像使用 Valve 官方 cdn.steamstatic.com 的已核实路径，文件名只从目录中的安全英雄标识推导。图像失败保留名称和分析数据。CSP 仅新增该精确图片域，脚本与 API 仍保持同源。没有新增 AI 或第三方分析服务。

## 当前能力核查

已实现的是历史画像分析：真实英雄池、五维百分位、英雄原型、当前顺序族的 BP 经验频率与来源覆盖。

完整 Phase A 尚未实现：Value 局面评估、Policy 在线下一手预测、Playbook 剧本、Advise 决策，以及战队级节奏和交手分析。models 目录没有模型实现，频率基线仅为离线评估；schema 和 fixture 不代表接口可用。天梯、最小回放导入、48 小时采集与位置真值验证也仍有缺口。

## 验证要求

新增前端行为先 RED 再 GREEN，覆盖缺值与零值、雷达闭合条件、摘要口径、代表英雄、图片路径安全、BP 并列。HTTP 回归检查图片 CSP、静态路由与接口边界。

主控实际浏览器检查总览、选手切换、矩阵跳转、双人比较、BP 过滤、能力说明、JSON 复制、真实不足状态与桌面/手机布局。交付时保留 GUI 与独占 PostgreSQL 55439，无后台采集，不部署到公网。GitHub 推送已获用户授权，不重复确认。


## 实施结果

7 项前端行为测试通过，23 项相关 Python 回归通过。实际浏览器验证桌面、中等窗口、手机布局，选手与比较图切换、矩阵跳转、BP 展开和过滤、缺值雷达、能力表、JSON 复制及真实样本不足。窄窗口与手机页面没有横向溢出。GUI 与独占 PostgreSQL 保留运行，用户最终视觉验收待完成。
