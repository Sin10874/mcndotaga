# Git worktrees (isolated workspaces)
.worktrees/

# Brainstorming visual companion artifacts (local only)
.superpowers/

# Secrets — never commit API keys
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/

# Node
node_modules/
dist/
.vite/

# Data — never commit replays or bulk datasets
data/
*.dem
*.dem.bz2
*.dem.zst
!data/.gitkeep

# Kaggle 引导数据集的 506.2 MB 子集：由 `python -m ingest.kaggle_subset` 落到此处，绝不入库
tests/fixtures/kaggle/

# Local runtime
*.log
.DS_Store
Thumbs.db
